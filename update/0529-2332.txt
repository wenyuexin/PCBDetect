修复：
1. 多次初始化使得部分信号与槽函数多次connect，导致进程收到某些信号后，相关槽函数多次执行的问题

修改：
1. 对于检测或提取界面，前期版本中，如果最后最后一行分图显示结束后仍未开始检测或提取，则直接将返回按键设为可点击。此版本修改了开启的时间，由之前的最后一行分图显示结束修改为运动复位之后

增加：
1. 若复位结束后未开始检测或提取，则启用检测或提取界面的开始键
2. 在产品序号识别界面的初始化函数和重置函数中，添加了设置鼠标样式的代码
3. 在产品序号识别界面的重置函数中，添加了清空掩膜区域坐标的功能
4. 对于产品序号识别界面，若仅使用鼠标修改掩膜区域坐标，则每次鼠标点击后直接修改runtimeParams中的掩膜区域坐标（相当于更新了坐标后，自动点击了确认区域的按键）